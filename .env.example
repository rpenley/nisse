DATABASE_URL=postgres://nisse:nisse@localhost:5432/nisse
REDIS_URL=redis://localhost:6379
PORT=8080

# Set by docker-compose automatically when running containerised.
# Only needed if overriding the default for host-side development.
# BACKEND_URL=http://backend:8080
